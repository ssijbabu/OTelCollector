apiVersion: v1
kind: ServiceAccount
metadata:
  name: otel-gateway
  namespace: otel-gateway
  annotations:
    # Azure Workload Identity — set this to the Client ID of the managed identity
    # assigned to this gateway (must have EventHubs Data Receiver role on the namespace)
    azure.workload.identity/client-id: <AZURE_CLIENT_ID>
