apiVersion: v1
kind: ServiceAccount
metadata:
  name: otel-collector
  namespace: otel-collector
  annotations:
    # Azure Workload Identity — set this to the Client ID of the managed identity
    # assigned to this collector (must have EventHubs Data Sender role on the namespace)
    azure.workload.identity/client-id: <AZURE_CLIENT_ID>
